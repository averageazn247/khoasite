<% provide(:title, 'Uploading') %>


<h1> In Attachment 1 </h1>
 
 
 
<%= form_for :wordfile, :html => { :multipart => true } do |f| %>
  Upload file: <%= f.file_field :word  %>
  <br />
  <%= f.submit( "Upload file" ) %>
  
  
   
<%= end %>