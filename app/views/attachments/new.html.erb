<% provide(:title, 'Uploading') %>


<h1> In Attachment 1 </h1>
 
 
 
<%= form_for @attachment, :html => { :multipart => true } do |f| %>
  Upload file: <%= f.file_field( "form", "file" ) %>
  <br />
  <%= f.submit( "Upload file" ) %>
  
  
   
<%= end %>